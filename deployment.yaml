apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-app
  labels:
    app: workflow-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: workflow-app
  template:
    metadata:
      labels:
        app: workflow-app
    spec:
      containers:
        - name: workflow-app
          image: udhaya53/workflow-app:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080

          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"

          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10

          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: workflow-app-service
spec:
  type: NodePort
  selector:
    app: workflow-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007
